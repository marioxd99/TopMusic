{% extends 'base.html.twig' %}
{% block body %}
<h1>Songs</h1>
    {{ form_start(form) }}
        {{ form_row(form.title) }}
        {{ form_row(form.Artist) }}
    {{ form_end(form) }}

{% endblock %}